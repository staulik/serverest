name: Automation - Python>>>Behave>>>Pytest

on:
  repository_dispatch:
    types: [deploy-regressiontest]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v2

      - name: Configurar Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Instalar dependências Python
        run: |
          python -m pip install --upgrade pip
          pip install behave selenium faker webdriver-manager allure-behave

      - name: Executar testes Behave com evidências Allure
        run: |
          cd aut_front
          mkdir -p allure-results
          behave -f allure_behave.formatter:AllureFormatter -o allure-results -f pretty || echo "tests_failed=true" >> $GITHUB_ENV

      - name: Instalar Allure CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y default-jre
          wget https://github.com/allure-framework/allure2/releases/download/2.27.0/allure-2.27.0.tgz
          tar -zxvf allure-2.27.0.tgz
          sudo mv allure-2.27.0 /opt/allure
          sudo ln -s /opt/allure/bin/allure /usr/bin/allure

      - name: Gerar relatório Allure
        run: |
          cd aut_front
          allure generate allure-results -o allure-report --clean

      - name: Compactar relatório Allure
        run: |
          cd aut_front
          zip -r allure-report.zip allure-report

      - name: Criar corpo do e-mail dinamicamente
        run: |
          if [[ "${{ env.tests_failed }}" == "true" ]]; then
            echo "📊 Relatório de Testes Automatizados - Behave (Allure)\n❌ FALHAS detectadas.\nConfira o anexo." > email_body.txt
          else
            echo "📊 Relatório de Testes Automatizados - Behave (Allure)\n✅ Todos os testes passaram!\nConfira o anexo." > email_body.txt
          fi

      - name: Enviar relatório por e-mail
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: Relatório de Testes Automatizados - Behave (Allure)
          to: silvio.aulik@gmail.com
          cc: staulik10@gmail.com, qatadeu@gmail.com
          from: ${{ secrets.EMAIL_USERNAME }}
          secure: true
          content_type: text/plain
          body: file://email_body.txt
          attachments: aut_front/allure-report.zip

      - name: Finalização
        run: echo "✅ Pipeline finalizada com envio do relatório Allure!"
























