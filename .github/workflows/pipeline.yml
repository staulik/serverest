name: Automation - Python>>>Behave>>>Pytest

on:
  repository_dispatch:
    types: [trigger-from-stage]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v2

      - name: Configurar Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install behave behave-html-formatter
          pip install selenium
          pip install faker
          pip install webdriver-manager

      - name: Rodar os testes com Behave + gerar relatório HTML
        run: |
          cd aut_front
          mkdir -p reports
          behave -f behave_html_formatter:HTMLFormatter -o reports/behave_report.html

      - name: Upload do relatório HTML como artefato
        uses: actions/upload-artifact@v4
        with:
          name: Relatorio-Behave
          path: aut_front/reports/behave_report.html

      - name: Exibir mensagem de sucesso
        run: echo "🎉 Testes executados e relatório gerado com sucesso, chefe! ✅"











