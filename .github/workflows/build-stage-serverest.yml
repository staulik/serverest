name: Build Stage Serverest test

on:
  push:
    branches:
      - stage-serverest

permissions:
  contents: read
  actions: write  # isso permite usar o repository_dispatch

jobs:
  trigger_stage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Simular deploy no Stage
        run: echo "🔧 Simulando deploy no ambiente stage"

  chamar_testes:
    needs: trigger_stage
    runs-on: ubuntu-latest
    steps:
      - name: Disparar pipeline de testes da main
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: saulik/serverest
          event-type: trigger-from-stage


