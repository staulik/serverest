name: BBVA - Build Stage Serveres

on:
  push:
    branches:
      - stage-serverest

permissions:
  contents: write
  actions: write  # Permite o uso do repository_dispatch

jobs:
  deployDev:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v2

      - name: Simular deploy no ambiente de Stage
        run: echo "🔧 Simulando deploy no ambiente stage"

  RegressionTest:
    needs: deployDev
    runs-on: ubuntu-latest
    steps:
      - name: Disparar pipeline de testes de regressão
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.PERSONAL_TOKEN }}
          repository: staulik/serverest
          event-type: devDeploy_qaRegressionTest





