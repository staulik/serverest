name: Build Stage Serverest

on:
  push:
    branches:
      - stage-serverest

jobs:
  trigger_stage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Simular deploy no Stage
        run: |
          echo "🔧 Simulando deploy no ambiente stage"

      - name: Criar commit vazio e push
        run: |
          git config user.name "ci-bot"
          git config user.email "ci-bot@serverest.com"
          git commit --allow-empty -m "gatilho stage concluído"
          git push origin stage-serverest
